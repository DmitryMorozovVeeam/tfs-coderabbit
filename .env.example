# =============================================================================
# GitLab Configuration
# =============================================================================
# Copy this file to .env and customize for your environment

# --- GitLab Settings ---
GITLAB_HOSTNAME=gitlab.local
GITLAB_HTTP_PORT=8081
GITLAB_SSH_PORT=2244
GITLAB_ROOT_PASSWORD=ChangeMe123!
GITLAB_TIMEZONE=UTC

# =============================================================================
# TFS / Azure DevOps Integration  (filled in by -TFSSetup)
# =============================================================================

# TFS server base URL including collection, e.g.:
#   https://tfs.company.com/tfs/DefaultCollection
#   https://dev.azure.com/orgname
TFS_URL=

# Team project name inside the collection
TFS_PROJECT=

# Personal Access Token with Code (read) and Pull Request (read) scopes
TFS_PAT=

# Comma-separated list of repo names to mirror (leave empty = mirror all repos)
TFS_REPOS=

# GitLab PAT created by -TFSSetup for the sync container (api scope)
GITLAB_TFS_TOKEN=

# GitLab group where mirrored repos will be created
GITLAB_TFS_NAMESPACE=tfs-mirrors

# Seconds between sync cycles (default: 60)
TFS_SYNC_INTERVAL=60
